RESPONSE FROM API->

{
  "datetime": "2024-04-20T15:00:00",
  "message": "Team meeting to discuss Q2 targets"
}

DOWNLOAD .msi file from 

https://github.com/tporadowski/redis/releases
 
and run that 

INSIDE VENV
celery -A app.ai_models.reminders.celery_config.celery_app worker --loglevel=info --pool=solo
python server.py

INSIDE CMD PROMPT
1.redis-server.exe redis.windows.conf

2.redis-cli config set stop-writes-on-bgsave-error no
redis-cli config set save ""
-------------------------------

In the main file we can call the function ->

from app.ai_models.reminders.tasks import remind_user
from datetime import datetime

# Just call it directly!
remind_user.apply_async(
    args=[user_id, message],
    eta=datetime.fromisoformat(remind_at)
)